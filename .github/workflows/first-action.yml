name: My First workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      # Runs a single command using the runners shell
      - name: Print Greeting
        run: echo "Hello, world!"
      # Runs a set of commands using the runners shell
      - name: Print good-bye
        run:  echo "Print Good-bye"
